# Allow build scripts for native modules
allow-build-scripts=@whiskeysockets/baileys,sharp,esbuild,protobufjs,fs-ext,node-pty,@lydell/node-pty,@matrix-org/matrix-sdk-crypto-nodejs

# Force HTTPS protocol for git operations (fixes SSH permission errors with libsignal-node)
git-protocol=https

# Increase timeouts for slow networks or GitHub rate limiting
fetch-timeout=120000
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000

# Better performance and error messages
loglevel=warn
prefer-offline=true
